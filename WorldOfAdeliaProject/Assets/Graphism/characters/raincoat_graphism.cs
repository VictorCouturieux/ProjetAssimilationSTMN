using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class raincoat_graphism : MonoBehaviour {

    public Material mat_persos;
    private int numbercolor = 0;
    public int Numbercolor {
        get { return numbercolor; }
        set { numbercolor = value; }
    }

    // Use this for initialization
    void Start() {
    }

    // Update is called once per frame
    void Update() {
        //If space press ---> color change
//        if (Input.GetKeyDown(KeyCode.Space)) {
//            numbercolor++;
//            if (numbercolor > 9) {
//                numbercolor = 0;
//            }
//        }

        //Color Palette

    }
}